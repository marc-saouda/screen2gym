# RL environment dependencies.
# The simulated-apps server uses only the Python standard library (http.server),
# so these are for the harness + judge.

playwright>=1.40        # browser automation (Chromium); see README for the cert workaround on download
requests>=2.28          # backend-mode HTTP + health checks
openai>=1.40            # LLM-based success judge (gpt-4o-mini); optional at runtime
python-dotenv>=1.0      # load OPENAI_API_KEY from the workspace-root .env
gymnasium>=0.29         # thin Gymnasium adapter (gym_env.py); one env id per episode

# Verified working versions in this environment:
#   playwright 1.60.0, openai 2.7.2, requests, python-dotenv, gymnasium 1.3.0
# Install (Homebrew python is PEP 668 "externally managed"):
#   python3 -m pip install --break-system-packages -r requirements.txt
#   NODE_TLS_REJECT_UNAUTHORIZED=0 python3 -m playwright install chromium
